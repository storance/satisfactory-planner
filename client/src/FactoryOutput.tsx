import { createSignal, For } from 'solid-js';
import { Select } from "@thisbeyond/solid-select";
import { GameDatabase, Item } from './game';


export default function FactoryOutput(props: { gameDB: () => GameDatabase, factoryIndex: number, outputIndex: number }) {
    return (
        <>
        </>);
}